FROM python:3.11-slim

WORKDIR /app

# Copiamos el core y la configuración común
COPY ./core /app/core
COPY ./config /app/config

# Instalamos dependencias básicas (se pueden agregar luego)
RUN pip install --no-cache-dir fastapi uvicorn httpx tenacity

